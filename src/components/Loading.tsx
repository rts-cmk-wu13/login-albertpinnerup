import Layout from "../Layout";

export default function Loading() {

    return (
        <Layout>
            <h2>Loading...</h2>
            <p>Please wait while we fetch the data.</p>
        </Layout>
    )

}